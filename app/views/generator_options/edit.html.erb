<% content_for :title, "Editing generator option" %>

<h1 class="font-bold text-4xl mbe-4">Editing generator option</h1>

<%= render "form", generator_option: @generator_option %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Show this generator option", @generator_option, class: "btn" %>
  <%= link_to "Back to generator options", generator_options_path, class: "btn" %>
</div>
