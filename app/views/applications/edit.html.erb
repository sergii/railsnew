<% content_for :title, "Editing application" %>

<h1 class="font-bold text-4xl mbe-4">Editing application</h1>

<%= render "form", application: @application %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Show this application", @application, class: "btn" %>
  <%= link_to "Back to applications", applications_path, class: "btn" %>
</div>
